package com.lob.springboot_api.controller;

public class AccountController {
    // 로그인 페이지 컨트롤러
    // request / code = L / code_explain = LOGIN
    // user / userID = userID / HR_ORGAN = HR_ORGAN / USERNAME = USERNAME
    // requestID / Generate value +1 / userID user.userID / requestCode = requestcode.requestCode / createDate = dateTime

    // AccountService.join(user)
    // requestService.requestForAccount(UserDTO.getUserID,"L")
}
