package com.lob.springboot_api.controller;

public class HomeController {
    // 인덱스 페이지 컨트롤러
    // request code = GI / code_explain = GETINDEX
    // requestID Generate value +1 / userID guests / requestCode = requestcode.requestCode / createDate = dateTime

    // requestService.requestForGetIndexPage("guests","GI") Logic을 진행할 때 DATE 생성 (datetime format YYMMDDhhmm)
}
